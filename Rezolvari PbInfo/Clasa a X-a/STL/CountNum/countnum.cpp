#include<iostream>
#include<vector>

using namespace std;

vector<int> CountNum(vector<int> &a, int S) {

}

int main() {
    vector<int> v;
    int sum;
    cin >> sum;
    for(int i = 0; i < 10; i++) {
        v.push_back(i);
    }
    CountNum(v, sum);
}